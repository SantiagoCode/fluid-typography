{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass",
    "sass:watch": "sass --watch styles/main.scss styles/styles.css"
  },  
  "dependencies": {
    "sass": "^1.62.1"
  }
}
